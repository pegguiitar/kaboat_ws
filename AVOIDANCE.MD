# 장애물 회피 — 알고리즘과 파라미터

부표 밭 같은 장애물 구역을 부딪히지 않고 통과하는 기능의 설명서.
코드에서 이 기능의 이름은 **`avoid`** 다 — 미션 상태 이름이자 명령 토픽 이름이고,
노드 파일은 `obstacle_planner.py` 다.

라이다로 만든 점유 지도를 **위험도 지도**로 바꾸고, 그 위에 **부드러운 곡선
경로**를 그린 뒤, 그 경로를 따라가며 매 순간 안전을 다시 확인한다.

값과 동작의 원본은 코드다 (`kaboat_behaviors/` 의 `dri.py`, `bspline_planner.py`,
`avoid_fsm.py`). 이 문서는 "어떻게 동작하고, 그 값이 왜 그 값인지"를 모아둔다.
관련: [SKELETON.MD](SKELETON.MD) §4(노드 구조)·§6(속도·추력 실측값).

**입출력**

| 방향 | 토픽 | 쓰임 |
|---|---|---|
| 구독 | `/occupancy_grid` | LiDAR log-odds 점유 지도 — 현재 뎁스캠 가중치는 0이라 장애물 증거에 기여하지 않음 |
| 구독 | `/odom` | 배 위치·방향·속도. **시간의 기준이기도 하다** (§6) |
| 구독 | `/mission/goal` `/mission/state` | 목표점 / 지금 이 behavior 차례인지 |
| 발행 | `/cmd/avoid` | 속도·회전 명령 → `cmd_mux` → `/cmd_vel` |
| 발행 | `/avoid/plan` `/avoid/cps` `/avoid/dri` | 디버그용 경로·제어점·목표점·위험도 지도 (RViz). **보는 사람이 있을 때만 발행** — RViz 를 끄면 만들지도 않는다 |

---

## 1. 전체 흐름

```
/scan ─▶ ① log-odds 점유 지도 ─▶ ② DRI 위험도 ─▶ ③ B-spline 생성 ─┬▶ ④ 따라가기+검사 ─▶ /cmd/avoid
           (10Hz 누적)          (매 tick)             ▲             │        │
                                                     │ 다시 만들기  │ 수리   │ 위험
                                                     └────────────┴───────┘
                                                     (앞쪽에 답이 없으면 → 후진 탈출)
```

10Hz 로 돈다. 매 tick 위험도 지도를 새로 계산하고, 지금 따라가는 경로가 아직
안전한지 검사한다. 안전하면 그대로 따라가고, 위험해지면 **수리 → 다시 만들기 →
후진 탈출** 순서로 대응한다. 어느 경우든 tick 마다 명령을 반드시 하나 발행한다.

## 2. 코드 구조

역할이 곧 파일 경계다. DRI·B-spline·FSM 세 라이브러리는 ROS를 쓰지 않아서
(`rclpy` 무의존) 가짜 시계와 가짜 장면으로 단위 테스트할 수 있다.

| 파일 | 역할 |
|---|---|
| `occupancy_grid.py` | 센서 증거 — LiDAR hit/free를 log-odds로 누적해 `/occupancy_grid` 발행 |
| `dri.py` | 위험 표현 — 점유 지도를 위험도 숫자 격자로. 시간·경로를 모름 |
| `bspline_planner.py` | 경로 기하 — 경로를 만들고/검사하고/수리. 시간·상태를 모름 |
| `avoid_fsm.py` | 조종 정책 — 상태(따라가기/탈출)와 시간을 갖는 유일한 곳 |
| `obstacle_planner.py` | ROS 노드 — 구독·발행·파라미터 배선만, 판단 로직 없음 |

주요 함수:

```python
build_dri(grid_msg, boat_xy, yaw, DriParams) -> DriGrid    # 위험도 지도
generate(dri, boat_xy, yaw, vel_xy, wp_xy, PlannerParams, now_t) -> Plan | None
check(plan, dri, boat_xy, p) -> int | None      # 위험한 첫 지점 번호, 없으면 None
repair(plan, dri, violation_idx, p) -> bool     # 경로를 조금 비틂, 실패면 False
curvature_ahead(plan, window) -> float          # 앞쪽 구간에서 가장 심한 굽이
AvoidFsm.step(dri, pose, vel, yaw_rate, wp, now_t) -> StepOut   # tick 하나
```

노드는 위험도 지도를 만들어 `AvoidFsm.step()` 에 넘기고, 돌려받은 명령을
`/cmd/avoid` 로 발행한다. 디버그 그림(`/avoid/plan` 경로, `/avoid/cps` 제어점과
목표점, `/avoid/dri` 위험도 지도)도 같은 노드가 낸다 — **시각화 전용 노드를 따로
두지 않는다.** 위험도 지도를 여기서 이미 계산했기 때문이다: 별도 노드는 같은 걸
다시 계산해야 하고(2.6~9.4ms/tick), 파라미터도 따로 갖게 돼 "RViz 에 보이는 지도"와
"플래너가 실제로 쓰는 지도"가 갈리는 함정이 생긴다. 대신 발행은 구독자가 있을
때만 한다 — 구독자 확인은 0.004ms 인데 메시지 만들기는 0.8ms 라, RViz 를 끄면
그만큼 절약된다(플래너 CPU 6.0% → 4.8% 실측).

## 3. 알고리즘

### 3-0. 점유 격자와 log-odds (`occupancy_grid.py`)

LiDAR 한 스캔을 월드 좌표 격자에 투영한 뒤, 빔 끝점은 점유 증거, 배에서
끝점까지의 경로는 자유공간 증거로 누적한다.

```text
L(cell) ← clip(L(cell) + 측정 증거, L_MIN, L_MAX)
P(occupied) = 1 / (1 + exp(-L))
```

현재 값은 다음과 같다.

| 항목 | 이전 → 현재 | 의미와 변경 이유 |
|---|---:|---|
| `L_OCC` | `+0.85 → +0.85` | 유지. 처음 hit된 셀을 약 70%로 만들며, `hit_dedup=True`일 때 한 스캔에서 같은 셀은 한 번만 가산한다. 값을 키우면 파도 한두 프레임도 너무 빨리 장애물로 확정되므로 올리지 않았다 |
| `L_FREE` | `-0.4 → -0.6` | LiDAR 빔이 통과한 셀의 감소를 강화했다. 4회 hit된 셀(`L=3.4`, 약 97%)은 free 한 번 후 `L=2.8`, 약 94%가 되어 DRI 문턱 95 아래로 바로 내려간다 |
| `L_MIN` | `-2.0 → -4.5` | 여러 번 free로 관측된 셀에 더 강한 비점유 확신을 저장한다. 최저 점유확률은 약 12%에서 약 1.1%로 낮아진다. 이미 `-4.5`까지 내려간 셀은 DRI 문턱까지 다시 올라오려면 `L_OCC=0.85` hit가 약 9회 필요해 짧은 파도 hit에 덜 흔들린다 |
| `L_MAX` | `+4.35` 유지 | 지속 관측되는 실제 장애물은 약 99%에서 포화시켜 한두 번의 free miss에 바로 사라지지 않게 한다 |
| `L_OCC_CAM` | `+0.5 → 0.0` | 뎁스 포인트가 수면을 장애물로 만드는 영향이 커서 장애물 증거를 끈 상태다. 콜백과 `observed=True` 처리는 남아 있으므로 뎁스 전용 셀은 50으로 보일 수 있지만 DRI에는 들어가지 않는다 |

`L_FREE`는 “이번 스캔에서 hit되지 않은 모든 셀”에 적용되는 decay가 아니다.
실제로 LiDAR ray가 지나간 셀에만 한 스캔당 한 번 적용된다. 새 셀의 내부
log-odds는 여전히 0에서 시작하며, `observed=False`인 동안 OccupancyGrid에는
`-1`(unknown)로 발행된다. 한 번 관측된 뒤 log-odds가 0이면 `50`으로 발행된다.
시간만 지났다고 줄어드는 지수 decay는 현재 없다.

DRI가 보는 `95` 문턱과의 관계는 다음과 같다.

```text
0에서 시작: 1 hit→70, 2 hit→85, 3 hit→93, 4 hit→97
4 hit 직후 free 1회: 97 → 94  (L_FREE=-0.6, DRI에서 즉시 제외)
충분히 free로 포화: L=-4.5 → 약 1, 이후 약 9 hit에서 다시 DRI 진입
```

이 변경은 파도 오탐을 빠르게 지우고, 이미 비어 있다고 반복 확인한 해역에는
강한 hysteresis를 주기 위한 것이다. 반대급부로 실제 장애물이 기존 free 셀에
갑자기 들어오면 새 셀보다 확정이 늦어진다. `L_MIN`은 새 셀의 prior를 바꾸는
값이 아니라 누적 하한이라는 점도 구분해야 한다.

IMU roll/pitch가 10°를 넘으면 해당 스캔은 통째로 버린다. 5°와 3°처럼 더
엄격한 제한도 검토했지만 hit뿐 아니라 free 갱신까지 함께 멈춰 기존 오탐이
늦게 지워지는 문제가 있어 10°를 유지했다. 다만 뎁스 장애물 증거를 끄고
`L_FREE`·`L_MIN`으로 LiDAR 수면 오탐이 더 빨리 사라지게 만든 뒤에는 tilt
필터가 유일한 방어선이 아니므로 제한을 다시 조금 완화할 여지가 있다. 파도가
큰 구간에서 정상 스캔까지 자주 버려진다면 `10°→15°`처럼 단계적으로 올리되,
각 단계에서 수면 hit의 4회 누적 빈도와 DRI 유령이 다시 늘어나는지 확인한다.

### 3-1. 위험도 지도 (`dri.py`)

장애물이 있는 칸마다 **동그란 위험 구름**을 씌운 격자. 숫자가 클수록 위험하고,
안전 기준선(1.0)을 넘으면 "여기는 안 된다"는 뜻이다.

- 구름은 **동그랗다**. 찌그러진 모양을 쓰면 배마다 뻗어나가는 줄기가 생겨
  지도를 덮어버린다. 동그라면 구름 반경이 곧 "장애물과 벌릴 간격"이 된다.
- 구름이 겹치면 **더 큰 값**을 쓴다(합치지 않는다). 배는 가장 가까운 것에
  부딪히지 여러 개의 합에 부딪히지 않는다. 합산하면 지나갈 수 있는 3m 틈이
  "막힘"으로 잘못 판정되어 부표 밭 전체를 우회하게 된다.
- 아직 못 본 칸은 **빈 곳**으로 본다. 위험하게 보면 센서 범위 밖으로 못 나간다.
- 구름 크기와 진하기는 **배에서 가깝고 뱃머리 정면일수록 크고 진하다**. 멀거나
  옆에 있는 장애물은 연하게 그려 통로를 열어둔다.
- 배 위치의 함수라서 **매 tick 다시 계산**한다 (점유 지도 자체는 캐시).
- 점유확률이 `occ_threshold=95` 이상인 셀만 가우시안의 중심이 된다. 현재
  log-odds 설정에서는 깨끗한 연속 관측 기준 4 hit부터 들어오므로 3 hit
  수면 유령(약 93)을 DRI 단계에서 한 번 더 거른다.
- `sigma`는 점유확률 문턱이나 hit 횟수가 아니라 가우시안의 **공간 폭 상한**이다.
  현재 `0.5m`이며 이전 `0.6m`보다 16.7% 좁다. 부표 사이에서 위험 구름이
  겹쳐 통로를 닫는 현상을 줄이기 위해 낮췄다. 대신 경로가 장애물에 더 가까이
  붙을 수 있으므로 실제 최소 이격거리는 별도로 확인해야 한다.

각 장애물 셀의 실제 폭과 진폭은 거리 `g`와 전방성 `f`로 조절한다.

```text
sigma_eff =
    sigma × (1 + k_dist·g) × (1 + k_head·f)
          / ((1 + k_dist) × (1 + k_head))

amplitude = (1 + a_dist·g) × (1 + a_head·f)
```

따라서 `sigma=0.5m`는 코앞·정면 장애물의 최대값이고, 멀거나 측면이면 더
작아진다. 검사 기준 `safe_threshold=1.0`에서의 유효 위험 반경은 대략
`sigma_eff × sqrt(2 ln(amplitude))`이다.

### 3-2. 경로 만들기 (`generate`)

**제어점 6개**로 부드러운 곡선을 그린다. 시작점과 끝점은 반드시 지나가고,
출발 방향은 뱃머리 방향으로 고정된다.

- **p0** = 배의 현재 위치.
- **p1** = 배가 곧 있을 자리 (뱃머리 방향으로 `전진속도 × t_look`). 급하게 꺾는
  경로를 못 만들게 막아준다. 정지·후진 중이면 최소값으로 줄어든다.
- **p2~p5** = 배를 중심으로 한 동심원 4개 위에서 방향만 고른다. 반지름이 계속
  커지므로 경로가 되돌아오는 일이 구조적으로 없다. 원의 반지름은 `p1` 거리부터
  계획 끝 반경까지 **똑같은 간격으로 4등분**한다.

계획 끝 반경 `r_end`는 rolling grid 안에 완전히 들어오는 최대 내접 반경에서
`boundary_margin=0.5m`를 뺀 값이며, waypoint가 더 가까우면 waypoint 거리로
줄인다. 너무 짧아 제어점이 겹치지 않도록 `p1_len + 2·cp_min_gap` 하한도 둔다.

`r1`은 선수각 기준 ±100° 안의 점유 셀 중 거리와 전방성을 함께 고려해 고른 기준
장애물이다. 예전처럼 첫 원을 `r1`에 놓지는 않는다. 그렇게 하면 먼 장애물에서
네 원이 계획 끝에 몰리는 퇴화가 생겼기 때문이다. 현재 `r1`은 “기준 장애물을
통과했는가”와 “더 가까운 기준 장애물이 새로 생겼는가”를 판단하는 재생성
트리거로만 저장한다. 이 범위에 기준 장애물이 하나도 없으면 `p2~p5` 방위를 모두
waypoint 방향으로 두고 곧바로 직선 경로를 만든다.

각 원에서 후보를 고르는 방법:

1. **후보 생성** — 매 `generate()` 시점의 배 위치와 waypoint를 이은
   `wp_bearing = atan2(wp_y-boat_y, wp_x-boat_x)`를 중심으로 ±45°에 후보
   31개를 3° 간격으로 만든다. 이는 월드 절대축이나 현재 선수각 중심이 아니다.
   선수각은 `p1` 방향과 첫 원의 연속성 비용에만 들어간다.
2. **위험한 후보 제거** — 직전 제어점에서 후보까지 직선을 긋고 8점을 샘플링한다.
   선분 최대 DRI가 `safe_threshold × gen_margin`(기본 `1.0 × 0.85 = 0.85`)을
   넘으면 탈락시킨다. 점 하나만 보면 후보점은 안전해도 거기까지 가는 선분이
   위험 구름을 가로지를 수 있기 때문이다.
3. **±12° 탐색의 기준 후보 선정** — 남은 안전 후보에 다음 기하 비용을 계산한다.

   ```text
   geom_cost(b) =
       w_wp   × |b - waypoint 방위|
     + w_prev × |b - 직전 제어점 방위|
   ```

   기본 가중치는 `w_wp=1.0`, `w_prev=1.0`이다. 목표 방향과 직전 제어점
   방향을 동등하게 반영해, 장애물 회피 중에도 이전 진행 방향을 더 오래
   유지하고 제어점 사이의 불필요한 좌우 꺾임을 줄인다.
   위험해서 제거된 후보의 비용은 무한대로 놓고, `geom_cost`가 가장 작은
   **안전 후보**를 `geom_best`로 선택한다. 이것이 ±`dri_angle_slack`의 중심이다.
   즉 ±12°는 선수각·장애물 방위·전체 최소 DRI 후보 기준이 아니다.
4. **DRI 지역 탐색** — `geom_best` 방위 ±`dri_angle_slack`(기본 ±12°) 안에
   있으면서 안전 필터도 통과한 후보만 `allowed`로 남긴다. 이 후보들의 선분
   최대 DRI 중 최저값 `min_risk`를 구한다.
5. **위험이 비슷하면 직진 우선** — 최대 DRI가
   `min_risk + risk_straight_slack`(기본 `+0.03`) 이내인 후보는 사실상 같은
   위험으로 보고, 그중 `geom_cost`가 가장 낮은 후보를 최종 선택한다. 위험
   차이가 유의미할 때는 더 안전한 후보가 우선이고, 차이가 작을 때만
   직진성이 우선된다.

중요하게도 현재 위험 평가는 선분 8점의 **최대 DRI**다. 선적분 방식도
시험했지만 얇고 높은 위험을 긴 안전 구간이 평균으로 희석하고 선분 길이에
따라 값의 의미가 달라지는 문제가 있어 롤백했다. 최대값은 실제 B-spline
검사도 “한 점이라도 기준 초과면 위반”으로 보는 정책과 단위가 같다.

`p2`를 고를 때 “직전 제어점 방위”는 현재 선수각이고, `p3~p5`에서는 바로
앞에서 선택한 제어점의 방위다. 따라서 `geom_best`와 ±12° 탐색 중심은
각 원호에서 새로 계산된다.

후보는 현재 코드 기준 **목표 방향 중심 90° 범위**(±45°)에서만 찾는다.
뒤로 도는 우회는 탈출 상태가 맡는다. 어느 원에서든 남은 후보가 0이면 경로를
못 만들고 `None`을 낸다.

이 생성 정책이 만들어진 순서는 다음과 같다.

1. 후보점 하나의 DRI만 보던 방식에서 **직전 제어점→후보 선분의 최대 DRI**로
   바꿨다. 후보점은 안전해도 그곳까지 가는 구간이 위험 구름을 관통하는 문제를
   막기 위해서다.
2. 전체 후보에서 DRI 최저만 고르면 제어점이 부표 밭 밖의 DRI=0 영역으로
   과도하게 빠졌다. 그래서 먼저 목표 방향과 이전 진행 방향으로 `geom_best`를
   정하고 DRI 탐색을 그 주변 ±12°로 제한했다.
3. 최소 DRI 하나를 그대로 고르면 작은 수치 차이에도 좌우로 흔들렸다. 최소값
   `+0.03`까지는 동급으로 보고 그중 기하 비용이 가장 낮은 후보를 골라
   직진성을 회복했다.
4. 직전 방향 가중치 `w_prev`를 `0.5→1.0`으로 올려 목표 방향 가중치와 같게
   만들었다. 각 원에서 이전 제어점 방향을 더 오래 유지해 S자 잔물결을 줄인다.
5. 후보 탐색 폭은 넓은 전진 반평면에서 줄여 현재 코드상 ±45°다. 중심은 항상
   배→waypoint 방위이며, ±12°는 이 전체 부채꼴이 아니라 `geom_best` 주변의
   2차 DRI 탐색 범위다.

최종 제어점 `[p0...p5]`에는 clamped cubic B-spline을 적용한다. 곡선을 먼저
300점으로 조밀 평가한 뒤 `0.5 × occupancy resolution` 간격으로 호길이
재샘플링한다. 현재 해상도 0.2m에서는 경로 검사점 간격이 약 0.1m다. 따라서
생성 단계의 8점 선분 검사는 후보를 빠르게 거르는 근사이고, 실제 안전 판정은
완성된 곡선의 0.1m 샘플 전체를 검사하는 `check()`가 담당한다.

### 3-3. 검사와 수리 (`check` / `repair`)

경로를 만드는 것과 검사하는 것은 분리되어 있다. 따라가는 동안 매 tick 최신
위험도 지도로 **앞쪽 구간만** 훑어서, 기준선을 넘는 첫 지점을 찾는다.
(배가 지나온 만큼 검사 시작점이 앞으로만 이동한다.)

위험이 발견되면 수리를 시도한다 — 제어점 4개를 각각 좌우로 6°씩 움직인
**8가지를 전부 만들어 보고**, 앞쪽 최대 위험도가 **확실히 줄어드는** 것만
받아들인다. 확실히 줄어드는 것만 받으므로 수리가 무한 반복되지 않는다.
반지름은 건드리지 않아서 "항상 바깥으로 전진" 성질도 유지된다.

### 3-4. 상태기계 (`avoid_fsm.py`)

상태는 **따라가기(FOLLOW)** 와 **후진 탈출(ESCAPE)** 둘뿐이다.

**따라가기** tick 순서:

```
경로 없음 → 정지 + 경로 만들기 시도 (t_retry 넘게 실패하면 탈출)
경로 있음 → 검사
  ├ 안전 → 다시 만들 때가 됐는지 확인 (tick 당 최대 1회)
  │    (a)  배가 기준 장애물을 지나쳤다        (진행 거리 > 저장된 r1)
  │    (a') 더 가까운 위협이 나타났다          (지금 r1 < 저장된 r1 − δ)
  │    (c)  마지막 생성 후 t_backstop 경과
  │    → 성공하면 교체, 실패하면 기존 경로 유지 (아직 안전하므로)
  └ 위험 → 수리(최대 3번) → 안 되면 다시 만들기
       └ 그것도 실패하면 여유를 본다
            위험 지점까지 d_panic 이상 남음 → 기어가며 다음 tick 재시도
            그보다 가깝거나 t_retry 초과   → 후진 탈출
```

(a') 에는 "기준 장애물이 없다가 생긴 경우"도 포함된다. 더 가깝거나 정면인
위협이 나타나면 반드시 r1 이 뚝 떨어지므로, 어느 셀인지 추적할 필요 없이
값 비교 한 줄로 판정된다.

**후진 탈출**: 뒤쪽 절반에서 방향 후보를 뽑아 배에서 후보까지 직선의 최대
위험도가 **가장 낮은 쪽**으로 후진한다. 뒤쪽만 보는 이유는 세 가지 —
① 앞쪽은 이미 더 엄격한 시험(경로 전체 + 위험 후보 제외)에서 떨어졌는데
짧은 직선 시험만 통과하는 "입구만 깨끗한 막다른 골목"으로 들어가면 더 갇힌다,
② 뒤쪽은 방금 지나온 물이라 비어 있음이 보장된다, ③ 탈출의 목적은 이동이
아니라 **경로를 다시 만들 수 있는 상태로 되돌리는 것**이다(후진하면 `p1` 이
줄어들어 출발 방향 제약이 풀린다).

뒤쪽 후보가 전부 기준선을 넘으면(포위) 그때만 전방위로 넓힌다. 어느 범위든
**가장 안전한 쪽을 무조건 고르고, 기준선으로 후보를 거르지 않는다** — 탈출은
마지막 단계라 실패하면 낼 명령이 없기 때문이다.

복귀는 **최소 시간(escape_min_t)이 지나고 + 경로 만들기가 연속 5번 성공**해야
한다. 둘 다 걸어둔 이유는 후진 → 경로 성공 → 전진 → 다시 막힘이 반복되는
떨림을 막기 위해서다.

### 3-5. 속도와 조향

- **조향**: 경로 위에서 `lookahead` 만큼 앞선 점을 목표로 삼고, 기존
  `seek_goal()` 과 같은 PD 제어를 쓴다(게인도 동일 — 축소 선체가 발진해서
  뒤집히지 않는 실측 안전값). 목표점이 waypoint 보다 멀어지면 waypoint 로
  잘라낸다. 순수 FSM에는 도착 판정을 만들지 않는다. ROS 노드인
  `obstacle_planner`가 완료 신호를 보내고, 최종 미션 전환은
  `mission_manager`가 웨이포인트 2m 조건과 함께 판정한다.
- **감속**: 앞쪽 구간에서 가장 심한 굽이를 보고 `속도 = ω_max / 굽이` 로 미리
  줄인다. 현재 회피 상한 1.1 m/s에서도 낼 수 있는 회전은 0.52 rad/s라 회피 경로가
  요구하는 굽이를 **조향만으로는 물리적으로 못 따라간다**. PD 만 쓰면 오차가
  생긴 뒤에야 반응해서 늦는다.
- **속도 → 명령 변환**: 추력 비율과 속도는 비례하지 않는다(12N→1.48 m/s,
  6N→0.97 m/s). `speed_to_cmd()` 함수 하나에 몰아넣어 실물 전환 때 이 함수만
  교체하면 되게 했다. `v_full=1.48m/s`는 변환 곡선의 교정점이고, 실제 회피
  명령 상한 `v_max`는 경로 추종성을 위해 1.1m/s로 낮춰 사용한다.

### 3-6. 시험 후 제거하거나 보류한 방식

- **선분 DRI 선적분**: 최대 DRI 대신 선적분이 가장 작은 후보를 고르는 방식도
  시험했지만 현재는 최대 DRI 방식으로 롤백했다. 선적분은 얇은 고위험 구간을
  긴 저위험 구간이 희석할 수 있고, 선분 길이에 따라 값이 달라진다.
- **ESCAPE 전 1.5초 지속 확인**: 수면 DRI 스파이크로 즉시 ESCAPE하는 현상을
  줄이려고 넣었지만, 경로 갱신과 대응 자체가 1.5초 늦어져 제거했다. 현재는
  매 tick 즉시 검사·수리·재생성하며, `d_panic=3m` 안에서 모두 실패하면
  바로 ESCAPE한다.
- **수면 높이 기반 LiDAR hit 제거**: odom z와 IMU 자세로 빔 끝점 높이를
  계산하는 필터를 시험했지만 실제 장애물까지 지우는 역효과가 있어 제거했다.
- **시간 decay**: 관측이 없어도 모든 셀을 시간에 따라 0으로 당기는 방식은
  쓰지 않는다. 현재는 실제 free ray의 `L_FREE=-0.6`과 더 낮은 `L_MIN=-4.5`로
  관측 기반 확신만 누적한다.
- **엄격한 tilt 제한**: 5° 이하로 줄이면 파도 hit뿐 아니라 free 갱신까지
  멈춰 잔상이 늦게 사라졌다. 현재는 10°를 유지한다.

## 4. 파라미터

아래 표는 현재 실행 코드의 기본값이다. 실험 중인 값도 있으므로 최종 실물값으로
간주하지 않는다.

### 점유 격자 (`occupancy_grid.py`)

| 이름 | 값 | 뜻과 근거 |
|---|---:|---|
| `L_OCC` | `+0.85` | LiDAR hit 1회의 log-odds. 약 70%에 해당하며 현재 유지값 |
| `L_FREE` | `-0.6` | ray가 통과한 셀의 free 증거. `-0.4`보다 빠르게 수면 오탐을 DRI 문턱 아래로 내림 |
| `L_OCC_CAM` | `0.0` | 뎁스캠 장애물 가산 비활성화. LiDAR-only 비교 상태 |
| `L_MIN` / `L_MAX` | `-4.5 / 4.35` | 반복 free 확신 약 1.1%까지 저장 / 반복 hit 약 99%에서 포화 |
| `hit_dedup` | `true` | 같은 스캔의 여러 빔이 같은 셀을 때려도 LiDAR hit는 셀당 한 번만 가산 |
| `TILT_MAX` | `10°` | 현재 보수값. 정상 스캔 누락이 많으면 15°까지 단계적으로 완화 가능하나 수면 DRI 오탐을 함께 확인 |
| `publish_rate` | `10Hz` | LiDAR 10Hz와 동율. 20Hz 발행은 같은 지도를 반복할 뿐 증거 갱신을 늘리지 않음 |

### 위험도 지도 (`DriParams`)

| 이름 | 값 | 뜻과 근거 |
|---|---|---|
| `occ_threshold` | 95 | 이 값 이상인 칸만 장애물로 인정. 3 hit≈93은 버리고 4 hit≈97부터 DRI 중심으로 사용 |
| `sigma` | 0.5 m | 위험 구름 폭의 최대값. `0.6→0.5`로 줄여 좁은 통로에서 가우시안 중첩을 완화 |
| `dist_falloff` | 4.0 m | 거리에 따라 크기·진하기가 줄어드는 기준 거리 |
| `k_dist` / `k_head` | 0.3 / 0.3 | 구름 크기 줄이는 정도. 코앞·정면은 `sigma` 그대로, 멀고 옆일수록 작아진다. 반대로 키우는 방식은 정면 중거리 구름을 0.88m 로 불려 3m 틈을 닫아버렸다 |
| `a_dist` | 1.0 | 가까울수록 진하게 |
| `a_head` | 0.7 | 정면일수록 진하게. 0.3 은 차이가 안 보일 만큼 약했다 |

### 경로 (`PlannerParams`)

| 이름 | 값 | 뜻과 근거 |
|---|---|---|
| `t_look` | 1.0 s | `p1` 거리 = 전진속도 × 이 값. 5.0 은 경로에 뾰족한 꺾임을 만든다 |
| `p1_min_dist` | 1.0 m | 정지·후진 중 `p1` 하한 (출발 방향이 사라지지 않게) |
| `bearing_max` | 100° | 기준 장애물(r1) 후보를 이 각도 안에서만 찾음 |
| `k_bear` | 1.5 | r1 고를 때 정면 쪽에 주는 가중치. 0.5 면 방금 지나친 옆 부표가 계속 r1 을 차지한다 |
| `boundary_margin` | 0.5 m | 계획 끝 반경 = 격자 가장자리까지 거리 − 이 값. 0.5 로 줄여도 가짜 경계 위반 0 이고 계획 거리가 0.5m 길어진다 |
| `min_horizon` | 3.0 m | waypoint 가 코앞이어도 이만큼은 계획 |
| `cp_min_gap` | 0.5 m | 원들이 겹쳐 뭉개지지 않게 하는 최소 간격 |
| `search_fov` | 90° | 후보를 찾는 범위 = 현재 배 위치에서 waypoint를 보는 방향 중심 ±45° |
| `n_candidates` | 31 | 원 하나당 후보 개수 (= 현재 3° 간격) |
| `w_wp` / `w_prev` | 1.0 / 1.0 | DRI 탐색 중심을 정하는 기하 점수. 목표 방향과 직전 방향을 동등하게 반영해 직진성을 강화 |
| `dri_angle_slack` | 12° | 기하 최적 안전 후보 주변에서 최소 DRI 후보를 찾는 각도 반폭 |
| `risk_straight_slack` | 0.03 | 최소 max DRI와 이 값 이내인 후보 중 가장 직선적인 것을 선택 |
| `safe_threshold` | 1.0 | **유일한 안전 기준선.** 경로를 만들 때와 검사할 때가 같은 선을 쓴다 |
| `gen_margin` | 0.85 | 만들 때는 기준선의 85% 에서 자름. 직선으로 판정하는데 실제 곡선은 몇 cm 안쪽으로 붙어서 생기는 오차 흡수 |
| `repair_max` / `repair_dtheta` | 3 / 6° | tick 당 수리 횟수 / 한 번에 움직이는 각도 |
| `progress_window` | 2.0 m | 배가 어디쯤 왔는지 찾을 때 보는 앞쪽 범위 |

### 주행·탈출 (`FsmParams`)

| 이름 | 값 | 뜻과 근거 |
|---|---|---|
| `lookahead` / `kappa_window` | 2.0 / 2.0 m | 조향 목표점 거리 / 굽이를 미리 보는 거리 |
| `v_min` / `v_max` | 0.3 / 1.1 m/s | 감속 하한 / 현재 회피 추종 상한. 1.48m/s 교정 전속보다 낮춰 경로 이탈을 줄임 |
| `slow_radius` | 3.0 m | waypoint 이 거리 안에서 감속 시작 (`seek_goal` 과 동일) |
| `omega_max` | 0.52 rad/s | 낼 수 있는 최대 회전 속도 (실측 0.44~0.49) |
| `t_backstop` | 3.0 s | 아무 일 없어도 이 주기로 경로를 다시 만듦 |
| `d_panic` | 3.0 m | 위험 지점까지 이만큼도 안 남으면 즉시 탈출. 타력으로 멈추는 데 ~2m (실측) + 여유 |
| `t_retry` | 5.0 s | 경로 확보·위험 해소가 이만큼 막히면 탈출 |
| `delta_r1` | 1.0 m | 기준 장애물이 "바뀌었다"고 볼 거리 차이 |
| `escape_radius` / `escape_n_bearings` | 2.5 m / 31 | 탈출 방향 후보의 거리 / 개수 |
| `v_escape` | 0.5 m/s | 후진 속도. 후진 추력은 전진과 대칭(−1.48)이지만 보수적으로 1/3 |
| `escape_min_t` / `escape_n_ok` | 3.0 s / 5 | 복귀 조건 — 최소 탈출 시간 + 연속 성공 횟수 |
| `cmd_full` / `v_full` / `cmd_exp` | 0.101 / 1.48 / 1.64 | 속도 → 추력 명령 변환 곡선. 2점 실측으로 맞춘 지수 |

## 5. 돌려보고 검증하기

스크립트는 `scripts/demo/` 에 있다. 컨테이너 안에서는
`/workspace/kaboat_ws/scripts/demo/` 로 보인다.

**시작 전**: 호스트에서 `xhost +local:` 을 한 번 실행한다. headless 로 띄워도
GPU 라이다·뎁스 센서가 화면을 그리기 때문에, 이걸 빼먹으면 sim 서버가 시작
2초 만에 죽는다.

`sim_avoid.sh`는 시작 전에 남아 있는 `odom_gps_noise.py` 프로세스에 먼저
SIGTERM을 보내고, 최대 2초 뒤에도 남으면 SIGKILL로 정리한 다음 새 sim을 띄운다.
이 정리가 없으면 비정상 종료 때 살아남은 여러 노드가 각각 `/odom`에 잡음을
더해 화면상 노이즈가 과도해진다. 새 노드는 `odom_noise_sigma=0.25m`로 실행되어
설계상 위치 오차의 약 95%(2σ)가 0.5m 안에 들도록 한다.

터미널을 나눠서 순서대로 (긴 것은 `docker exec -d` 로):

```bash
scripts/demo/sim_avoid.sh      # sim — 부표 밭 서쪽(2, 75)에 스폰
scripts/demo/t2t.sh            # 드라이버 — 명령을 좌우 추력으로
scripts/demo/avoid_stack.sh    # 점유 지도 + 명령 먹스 + 회피 플래너
scripts/demo/mission_stub.sh   # 목표점 발행 (기본 = 밭 건너편 29, 76.5)
scripts/demo/rviz_avoid.sh     # 보기 — 경로·제어점·목표점·위험도 지도
```

**판정 스크립트** (목표점을 인자로 준다):

```bash
python3 scripts/demo/clearance_check.py 29.0 76.5   # 부표와의 최소 거리 = 접촉 판정
python3 scripts/demo/watch_goal.py      29.0 76.5   # 완주 시간·속도·전복 여부
```

접촉 판정 기준은 **최소 거리 0.5m 초과**(선체 반폭 0.3m + 여유). 이전 파라미터
조합의 7번 주행 기록은 0.48~0.90m였고, 그중 0.48m는 이 여유 기준에 못 미친다.
현재 `sigma=0.5m`, `search_fov=90°`, `v_max=1.1m/s` 조합의 최소 이격거리는
같은 조건으로 다시 측정해야 한다.

`meas_step1.py` 는 배를 정해진 순서로 몰며 전진·후진·회전 성능을 재는
스크립트다. **선체나 스러스터가 바뀌면 이것부터 돌려서** §4 의 속도·회전 값을
다시 채운다.

## 6. 알려진 한계

- **제어점 간격이 위험 구름보다 촘촘해야 한다.** 지금은 간격 ≈0.9m 로 성립하지만,
  격자를 키워 계획 거리가 길어지면 원 사이가 벌어져 장애물이 어느 원에도 안
  걸릴 수 있다. 그때는 원 개수(고정 4개)를 다시 봐야 한다.
- **눈앞만 보고 고른다.** 경로 만들기는 원마다 순서대로 최선을 고르고, 수리는
  주변만 조금 비튼다. 그래서 위험한 능선 너머에 있는 더 좋은 길로는 못 넘어간다
  (게이트+측면 장애물 장면에서 실측). 이 경우 수리 실패 → 다시 만들기 → 후진
  탈출로 빠져나온다. sim 에서 자주 발생하면 후보 점수 방식을 손봐야 한다.

## 7. 주의사항

- **tick 마다 명령을 반드시 발행할 것.** `cmd_mux` 는 300ms 동안 명령이 없으면
  그 behavior 를 무시한다(워치독). 경로를 다시 만드는 tick 에도 끊기면 안 된다.
- **시간은 `/odom` 스탬프를 쓸 것.** sim 은 시계가 둘(벽시계 / sim 시계)이고
  실시간보다 느리게 흘러서, 벽시계를 쓰면 RViz 가 좌표 변환을 못 해 경로·제어점
  표시가 통째로 사라지고 주기 판정도 어긋난다. 실물에서는 둘이 같다.
- **속도 명령은 SKELETON §6 안전값 범위 안에서.** 축소 선체(1.1m)는 관성이 작아
  회전 명령이 크면 뒤집힌다(실측 2회).
- **실물 전환 시 다시 재야 하는 값**은 SKELETON §6 재캘리브레이션 지도 참조 —
  추력·속도 변환, 최대 회전 속도, 제동거리에 걸린 `d_panic` 등.
